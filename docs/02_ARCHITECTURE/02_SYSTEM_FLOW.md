# HYB System Flow

현재:
User Query → Marketplace → Normalize → Matching → Price → Trend → Confidence → Opportunity → Recommendation → Storage

미래:
Scheduler → Collectors → History → Ranking → Recommendation → Alert/API

일부 Marketplace 실패는 전체 실패가 되지 않는다.
